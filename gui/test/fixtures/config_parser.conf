# Shared App / Root Helper parser fixture.
# HOST="ignored-comment.example:443"

HOST="vpn.fixture.example:8443"
USER=operator=west
GROUP="engineering"
SERVERCERT="pin-sha256:AbCdEf+/0123=="

# Comment and blank lines must not become configuration values.
