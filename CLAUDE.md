# CLAUDE.md — LiteOC

Engineering-skills configuration entrypoint for this repo.

## Agent skills

### Issue tracker

Issues & PRDs live as GitHub issues in `ren2019/LiteOC`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Five canonical roles: `needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one root `CONTEXT.md` (glossary) + `docs/adr/`. See `docs/agents/domain.md`.
